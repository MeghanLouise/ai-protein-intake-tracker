rules_version = '2';

// Locked down: browsers get no direct access. Only the API (Admin SDK, which bypasses rules)
// reads and writes data.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
